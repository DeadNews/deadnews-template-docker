name: Docker

on:
  push:
    branches: ["main"]
    tags: ["v*.*.*"]
  pull_request:
    branches: ["main"]

permissions:
  id-token: write
  packages: write
  security-events: write

jobs:
  docker:
    uses: ./.github/workflows/docker-reusable.yml
